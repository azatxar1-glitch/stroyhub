Кадры для секций «О компании» и «Каждый объект — это ответственность».

Пути указываются в `src/data/kamtehnostroy/company.ts`:
`about.image` и `statement.image`.

Рекомендации: широкий горизонтальный кадр, ширина 2400–3200 px.

Промпты для генерации — `docs/kamtehnostroy-images.md`.
